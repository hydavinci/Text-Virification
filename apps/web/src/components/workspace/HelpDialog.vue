<script setup lang="ts">
import AccessibleDialog from './AccessibleDialog.vue'

defineProps<{
  open: boolean
}>()

const emit = defineEmits<{
  close: []
}>()
</script>

<template>
  <AccessibleDialog
    :open="open"
    labelled-by="help-title"
    close-label="关闭使用帮助"
    close-data-attribute="data-close-help"
    @close="emit('close')"
  >
    <h2 id="help-title">使用帮助</h2>
    <p>
      上传文件或粘贴文本，选择文档场景，点击“开始检查”。选中文件不会自动提交。
      上传页卡片中的“检查设置”和审阅页右上角的同名入口，
      均可调整合规开关、术语表及禁用词。
      修改设置不清空当前文档；在审阅页修改后，关闭设置并点击“重新检查”应用。
      检查进行中仍可查看设置，完成后才能修改。重新检查只检查当前文字，
      更换 OCR 语言需重新上传文件。
      PNG/JPEG 图片也可上传；图片和扫描 PDF 可选择中英文、英文或日文 OCR，
      日文识别不包含日文纠错。图片导出为可编辑 Word，不覆盖原图。
      扩展检查可按需开启英文词典拼写与保守语法检查，以及中英文间距、空行和长句建议，长句只提示人工调整。
    </p>
    <p>检查覆盖字符、词汇、句子、标点格式、语篇语体和合规安全六个维度。结果供人工审阅，不替代专业判断。</p>
    <p>
      检查完成后可逐条接受、忽略或撤销建议，并通过查找替换和原文编辑完成最终校订。
      Word、RTF、PDF 和图片在同一个分页界面展示版式及问题标记，无需切换模式。
      点击问题直接定位原文；接受、撤销建议或保存编辑后更新版式，不覆盖上传文件。
      图片、扫描件的图中文字不能直接替换，修订保留在右侧并通过重建稿导出。
      Word 版式可能因字体或特殊对象兼容性存在差异。无法定位或更新预览时会明确提示，
      不会用纯文本冒充原文，也不会把旧页面当成最新修订。
      宽屏下从左到右为查找替换、正文、问题列表三栏；问题列表不再被查找工具挤占高度。
      可按 Ctrl/Cmd+F 聚焦查找框；手机端会切换到独立的查找替换面板。
      在查找框中按 Enter 定位下一项，Shift+Enter 定位上一项。
      “撤销修改”按操作顺序回退替换和已保存的原文编辑，全部替换算一步；刷新页面后仍可继续撤销。
      重新检查或更换文档会开始新的修改记录。旧会话缺少审阅快照时，回退正文后仍需重新检查。
      纯文本使用段落视图；关闭“显示问题标记”可阅读干净正文，不改变已接受的修改或导出内容。
    </p>
  </AccessibleDialog>
</template>
